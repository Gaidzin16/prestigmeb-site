<?php
/* Настройки приёма заявок. Скопировать в config.php и заполнить.
 * config.php в git не попадает (.gitignore) — там живут токены. */
return [
    /* Куда слать письмо с заявкой (можно несколько через запятую) */
    'mail_to'   => 'prestig-meb@mail.ru',
    /* От кого. Должен быть ящик на домене сайта, иначе mail.ru отправит в спам */
    'mail_from' => 'site@prestigmeb.ru',
    'mail_from_name' => 'Сайт Престиж',

    /* ВКонтакте: ключ доступа сообщества (Управление → Работа с API → Ключи доступа,
     * права «сообщения сообщества»). Пусто — уведомление в ВК не шлём. */
    'vk_token'  => '',
    /* Кому писать: id пользователя-администратора (число). Сообщество может писать
     * только тем, кто разрешил ему сообщения — админ должен один раз написать группе. */
    'vk_peer_id' => 0,

    /* Не больше N заявок с одного IP за окно (секунды) */
    'rate_limit' => 5,
    'rate_window' => 3600,

    /* Файл-журнал заявок (резерв на случай, если почта/ВК не дошли) */
    'log_file' => __DIR__ . '/leads.log',
];
